import { memo } from "react"

export const REVEAL_EVENT_TYPES = {
    revealed: 'executed-encrypted-tx',   
  }

export const REVEAL_EVENT_ATTRS = {
    creator: 'creator',
    price:   'price',
    index:   'index',
    memo:    'memo',
}